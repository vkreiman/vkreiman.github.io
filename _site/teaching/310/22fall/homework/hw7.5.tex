\documentclass[12pt]{exam}

\usepackage{fullpage}
\usepackage{amsmath,amssymb,amsfonts,amsthm,amscd}

%\pagestyle{myheadings}
%\markright{August 21, 2007}
%\usepackage{fancyhdr}
%\pagestyle{fancy}

\lhead{Univ. of Wisconsin - Parkside}
\chead{Math 310}
%\rhead{\today}
\rhead{Halloween, 2022}

\headrule
\pagestyle{headandfoot}
%\cfoot{}

\newcommand{\bN}{\mathbb{N}}
\newcommand{\bQ}{\mathbb{Q}}
\newcommand{\bR}{\mathbb{R}}
\newcommand{\bZ}{\mathbb{Z}}
\newcommand{\bC}{\mathbb{C}}

\newcommand{\cA}{\mathcal{A}}
\newcommand{\cB}{\mathcal{B}}
\newcommand{\cC}{\mathcal{C}}
\newcommand{\cP}{\mathcal{P}}
\newcommand{\cT}{\mathcal{T}}

\def\ni{\noindent}
\def\DS{\displaystyle}
\def\ol{\overline}

\def\vs{\vspace{1em}}

\makeatletter
\def\imod#1{\allowbreak\mkern5mu({\operator@font mod}\,\,#1)}
\makeatother

\begin{document}

\phantom{i} \vs

\begin{center}
	\begin{large}
          \textbf{Homework 7.5: Joint Distributions}
	\end{large}
\end{center}
%\vs

\begin{questions}


  \question Four cards are drawn from a deck. Let $X$ be the nuumber
  of kings drawn and $Y$ the number of queens. Find $f_{X,Y}(x,y)$.

  \question Two dice are tossed. Let $X$ be the number of 2's that
  appear and $Y$ the number of 3's. Write the matrix giving
  $f_{X,Y}(x,y)$. Define a third random variable $Z$ by $Z=X+Y$. Use
  $f_{X,Y}(x,y)$ to find $f_Z(z)$.

\question Let $X$ and $Y$ be continuous random variables with joint pdf 
\begin{equation*}
  f_{X,Y}(x,y) = 
  \begin{cases}
    2e^{-(x+y)}& 0<x<y, 0<y\\
    0& \text{otherwise}
  \end{cases}
\end{equation*}
Find $P(Y<3X)$.


% \question A point is chosen at random from the interior of a circle
% whose equation is $x^2+y^2\leq 4$. Let the random variables $X$ and
% $Y$ denote the $x$- and $y$-coordinates of sampled point. Find
% $f_{X,Y}(x,y)$.

% \question Suppose $f_{X,Y}(x,y)=xye^{-(x+y)}$, $x>0$, $y>0$. Prove
% that for any real numbers $a,b,c$, and $d$, $$P(a<X<b,
% c<Y<d)=P(a<X<b)\cdot P(c<Y<d),$$ thereby establishing the independence
% of $X$ and $Y$.

\end{questions}



\end{document}
